# Tabs

This folder contains the implementation for each workflow tab in the modular GUI:

- `setup_tab.py`: Setup/paths tab
- `search_select_tab.py`: Search & select tests tab
- `config_tab.py`: Configuration tab
- `generate_tab.py`: Report generation tab

Each tab is a class that builds its own content and is registered in `main_gui.py`.
