# Components

This folder contains reusable UI components and dialogs for the modular GUI.

- `base.py`: Base classes for components and tabs
- `dialogs.py`: Dialog components (SAP selection, notes, etc.)
